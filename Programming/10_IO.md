






## File Open


```python
# pip install pyyaml

import yaml

with open('test.yaml', encoding='UTF-8') as f:
    cfg = yaml.load(f, Loader=yaml.FullLoader)

KEY = cfg['host']
URL = cfg['']

print(KEY, URL)
```

